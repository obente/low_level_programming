#include "main.h"
#include <string.h>

/**
 * _strlen - check length of passed string
 * @s: string to be passed and counted
 * Return: Always length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
